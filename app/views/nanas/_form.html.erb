<%= form_with(model: nana) do |form| %>
  <% if nana.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nana.errors.count, "error") %> prohibited this nana from being saved:</h2>

      <ul>
        <% nana.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :profile_image, style: "display: block" %>
    <%= form.text_field :profile_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
